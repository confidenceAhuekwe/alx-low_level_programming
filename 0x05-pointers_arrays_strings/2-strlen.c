#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s: parameter of the function of type character.
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
