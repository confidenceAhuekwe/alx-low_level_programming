#include <stdio.h>

/**
 * main - output all single digit numbers of base 10 staring from 0
 * REturn: 0 if successful
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d\n", var);
	return (0);
}
